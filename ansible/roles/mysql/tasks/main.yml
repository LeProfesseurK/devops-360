---
# tasks file for mysql


- name: Install
  apt:
    name: mysql
    state: present
    cache_valid_time: 3600

#- name: passw user in mysql
- mysql_user:
    name: niki
    priv: '*.*:ALL, GRANT'
    state: present

- name: Create a new database 
  mysql_db:
    name: devops
    state: present
